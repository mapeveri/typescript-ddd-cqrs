apiVersion: v1
kind: ConfigMap
metadata:
  name: languages-app-config-map
  labels:
    app: languages-app
    role: variables
data:
  NODE_ENV: development
  SERVER_PORT: "4000"
  MONGO_DB_DATABASE: languages
  MONGO_DB_URL: mongodb://mongo1:27017/languages?replicaSet=rs0&directConnection=true&directConnection=true
  JWT_SECRET: "45wef4we566dwdwgv.fi)__d"
  FRONTED_URL: "http://localhost:3000"
  RABBITMQ_USER: app
  RABBITMQ_PASSWORD: rabbit_app
  RABBITMQ_EVENTS_QUEUE: events_queue
  RABBITMQ_HOST: amqp://app:rabbit_app@rabbitmq:5672
